# セキュリティポリシー

## このアプリケーションの性質

Music Tag Auditor はローカル完結のデスクトップアプリケーションである。

- **ネットワーク通信を一切行わない。** 外部サービスへの送信も、更新確認も、テレメトリの収集もしない
- 認証情報・API キー・トークンの類を持たない
- 読み書きするのは利用者が指定した音楽ファイルのタグと、`%APPDATA%\MusicTagAuditor\`（辞書・設定）および `%LOCALAPPDATA%\MusicTagAuditor\logs\`（ログ）のみ

したがって想定される脅威は、**利用者自身の音楽ファイルが意図せず破壊されること**が中心になる。これに対しては次の設計で対処している（[docs/SPEC.md](docs/SPEC.md) 9章）。

- 適用直前にタグのスナップショットを自動取得する
- 書き込んだ全項目を読み戻して照合する
- アプリを使わずに戻せる PowerShell 復元スクリプトをバックアップに同梱する

## サポート対象バージョン

`develop` ブランチの最新状態のみ。過去のコミットに対する修正提供は行わない。

## 脆弱性の報告

**公開 Issue には書かないこと。** GitHub の Security Advisories から非公開で報告してほしい。

1. リポジトリの **Security** タブを開く
2. **Report a vulnerability** を選ぶ

個人プロジェクトのため対応期限は約束できないが、報告は必ず確認する。

タグの破壊やデータ消失につながる不具合で、悪用の余地が無いものは通常の Issue でよい。
