namespace MusicTagger.Core.Tests;

/// <summary>
/// テストプロジェクトの疎通確認用のプレースホルダ。
/// 段階 1 以降で正規化・辞書引き・検査ルールのテストを追加したら削除する。
/// </summary>
public sealed class PlaceholderTests
{
    /// <summary>
    /// テストランナーが動作していることだけを確認する。
    /// </summary>
    [Fact]
    public void TestRunnerWorks()
    {
        Assert.True(true);
    }
}
